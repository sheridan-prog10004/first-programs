#ask the user for their name
userName = input("What is your name? ")

#create a "formatted string" by comgining the greeting, 'Hello' with their name stored in the variable "userName"
greeting = f"Hello {userName}!"

#output hello using their name (e.g. "Hello <...>") by 
print(greeting)